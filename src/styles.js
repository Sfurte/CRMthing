/**
 * Design system tokens extracted from Untitled UI Figma kit.
 * Shared across components to avoid duplicating style objects.
 */

/** Colors – background, text, border */
export const colors = {
  white: '#FFFFFF',
  background: '#F0F7FF',  // canvas area
  border: '#D4D4D4',
  borderLight: '#F8FBFF',
  divider: '#ACACAC',
  textPrimary: '#202020',
  textSecondary: '#525252',
  textMuted: '#999',
  hoverBackground: '#F0F7FF',
  blue: '#3B82F6',
};

/** Typography – font family, sizes, weights */
export const typography = {
  fontFamily: "'Inter', system-ui, sans-serif",
  sizeXs: 12,
  sizeSm: 14,
  sizeMd: 16,
  sizeLg: 20,
  weightNormal: 400,
  weightMedium: 500,
};

/** Spacing – padding, gap, margin values */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
};

/** Border radii */
export const radii = {
  sm: 6,
  md: 8,
  lg: 12,
};

/** Shadow */
export const shadows = {
  dropdown: '0px 4px 12px rgba(0,0,0,0.1)',
  input: '0px 1px 2px rgba(0, 0, 0, 0.05)',
};

/** Layout constants */
export const layout = {
  headerHeight: 60,
  toolbarHeight: 44,
  leftPanelWidth: 359,
  artboardWidth: 1440,
  artboardHeight: 900,
};
