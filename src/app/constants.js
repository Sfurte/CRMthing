/**
 * Constants used across the canvas and zoom/pan system.
 */

/** Width of the white artboard (in canvas-space pixels) */
export const ARTBOARD_WIDTH = 1440;

/** Height of the white artboard */
export const ARTBOARD_HEIGHT = 900;

/** Minimum zoom level (e.g. 0.2 = 20%) */
export const MIN_ZOOM = 0.2;

/** Maximum zoom level (e.g. 5 = 500%) */
export const MAX_ZOOM = 5;
